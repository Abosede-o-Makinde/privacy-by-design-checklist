"""Temporary probe — remove after the Actions failure email is confirmed."""


def test_temporary_actions_failure_for_email() -> None:
    assert False, "temporary probe: confirm GitHub Actions failure email"
